SELECT 
i.target_slug, 
i.slug, 
i.code as indicator_code, 
i.title as indicators_title, 
t.title as target_title 
FROM {{indicators_table}} i JOIN {{targets_table}} t ON i.target_slug=t.slug 
order by 
  i.code
