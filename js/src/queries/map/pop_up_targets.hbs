SELECT 
  i.slug,
  i.title,
  s.score, 
  s.iso 
FROM indicators i JOIN scores s ON s.indicator_slug=i.slug 
WHERE i.target_slug = '{{slug}}' 
AND 
s.iso LIKE (SELECT iso FROM countries WHERE st_intersects(the_geom, st_setSrid(ST_MakePoint({{lng}}, {{lat}}),4326))) 
