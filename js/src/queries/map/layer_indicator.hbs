select 
  c.the_geom, 
  st_transform(st_makevalid(c.the_geom_webmercator), 4035) as the_geom_webmercator, 
  s.iso, 
  s.score  
FROM scores as s JOIN countries as c on c.iso = s.iso 
WHERE indicator_slug='{{slug}}'
