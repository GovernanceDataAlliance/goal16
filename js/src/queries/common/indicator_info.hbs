SELECT   
  i.title,   
  i.description,   
  i.slug,  
  best_score,   
  worst_score,   
  units,   
  s.name as source_name,
  s.link as source_link 
FROM  
  {{table}} i JOIN sources s on i.source_slug=s.slug 
WHERE   
  i.slug='{{indicator_slug}}'
