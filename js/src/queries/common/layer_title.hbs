SELECT   
  i.title, 
  i.slug as slug, 
  i.code  
FROM  
  {{indicators_table}} i 
WHERE   
  slug='{{slug}}' 
UNION 
SELECT 
  t.title, 
  t.slug as slug,
  t.code  
FROM 
 {{targets_table}} t 
WHERE   
  slug='{{slug}}'
