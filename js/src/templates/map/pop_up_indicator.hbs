<div class="m-popup">
  <button class="btn-close">
    <svg class="icon icon-close">
      <use xlink:href="#icon-close"></use>
    </svg>
  </button>
  <div class="wrapper">
    <a href="countries/FRA"><h1 class="c-title-popup -small">{{country.name}}</h1></a>
    {{#each indicators}}
      <div class="data">
        {{!-- <p>{{title}}</p> --}}
        <p>Last data point: {{year}}</p>
      </div>
      <div class="score c-score-popup">{{#comma score}}{{/comma}}<span class="units">{{ units_abbr }}</span></div>
    {{/each}}
    <div class="links">
      <a href="{{url}}/countries/?iso={{country.iso}}">visit country page > </a>
      <a href="{{url}}/compare/?isoA={{country.iso}}">compare to others > </a>
    </div>
  </div>
</div>
<div id="popup-background"></div>
